# cloakbrowser ships frequent patches (stealth/fingerprint fixes for live bot
# detection) that we want promptly. Exclude it from the global 7-day
# minimum-release-age supply-chain gate so we can track its latest release.
# The gate still applies to every other dependency.
minimum-release-age-exclude[]=cloakbrowser
